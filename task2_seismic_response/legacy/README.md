# Legacy provenance

Copy historical first-submission scripts here unchanged. Do not make them dependencies of the rebuilt pipeline. Files containing `_0` may be archived externally and should be excluded from the canonical public package.
