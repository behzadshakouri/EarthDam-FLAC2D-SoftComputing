# Data

The canonical workflow expects private local copies of `outputs_gp.mat` and
`PT1_FoS_SCT.xlsx`. Large raw and generated files are excluded from Git.
Configure their locations in `config/task1_user_paths.m`.

See `../docs/RAW_DATA_CONTRACT.md` for exact dimensions and ranges.
